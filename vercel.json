{
  "buildCommand": "npm install -g vite && cd backend/hvac-service-app && npm install && npm run build",
  "outputDirectory": "backend/hvac-service-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}